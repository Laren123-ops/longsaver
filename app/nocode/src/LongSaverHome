// app/nocode/src/LongSaverHome.jsx
export default function LongSaverHome({ user }) {
  return (
    <div style={{ padding: 24 }}>
      <h1>LongSaver</h1>
      <p>已登录：{user?.email}</p>

      {/* 这里粘贴/渲染你原来做好的页面内容 */}
    </div>
  )
}
